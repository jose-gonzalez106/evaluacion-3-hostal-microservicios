spring:
    main:
        allow-bean-definition-overriding: true
    application:
        name: hostal
    datasource:
        url: jdbc:mysql://${DATABASE_HOST:localhost}:${DATABASE_PORT:3306}/hostaldb?useSSL=false&serverTimezone=America/Santiago&createDatabaseIfNotExist=true
        username: ${DATABASE_USERNAME:root}
        password: ${DATABASE_PASSWORD:}
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQLDialect
                format_sql: true

server:
    port: 8082

springdoc:
    api-docs:
        enabled: true
        path: /hostal/api-docs
    swagger-ui:
        enabled: true
        path: /swagger-ui/index.html

logging:
    level:
        com.example.hostadmin: DEBUG
        org.hibernate.SQL: DEBUG

eureka:
    client:
        register-with-eureka: true
        fetch-registry: true
        service-url:
            defaultZone: ${EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE:http://localhost:8761/eureka/}
